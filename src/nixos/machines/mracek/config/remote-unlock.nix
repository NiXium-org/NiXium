{ ... }:

# Implementation of a remote-unlock for MRACEK

{
	# FIXME(Krey): Can reportedly be used to enter the passphrase
	# boot.initrd.network.ssh.enable = true;
	# boot.initrd.network.enable = true;
}